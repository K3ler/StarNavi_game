const initialState = {
    name: '',
    isRunning: false
}

const Map = (state = initialState, action : any) => {

    switch (action.type) {
        default:
            return state
    }
}

export { Map } 