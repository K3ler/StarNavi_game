const initialState = {
    userScore: 0,
    aiScore : 0,
    userName: ''
}

const Player = (state = initialState, action: any) => {

    switch (action.type) {
        default:
            return state
    }
}

export { Player } 